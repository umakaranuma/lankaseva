# Production environment — loaded when main.dart's kEnvironment = AppEnv.production
#
# Committed to git. Bundled into the app and extractable from a release build —
# URLs / public config only, NO real secrets.

# Backend origin — NO trailing slash, NO /api suffix. Set the real host.
API_BASE_URL=https://api.lankaseva.lk

# MAPS_API_KEY=
# SENTRY_DSN=
